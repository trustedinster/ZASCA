<!DOCTYPE html>
<!DOCTYPE html>
<html lang="zh-CN" {% if is_demo_mode %}data-demo-mode="true"{% endif %}>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登录 - ZASCA</title>
    
    <!-- Material Icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    
    <!-- MD3 Theme -->
    <link rel="stylesheet" href="/static/css/theme.css">
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/static/img/favicon.png">
    
    <style>
        :root {
            --login-bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --login-card-bg: var(--md-sys-color-surface);
            --login-accent: var(--md-sys-color-primary);
        }
        
        body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: var(--login-bg-gradient);
            font-family: var(--md-sys-typescale-body-large-font-family, 'Roboto', sans-serif);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .login-container {
            width: 100%;
            max-width: 400px;
            padding: 24px;
        }
        
        .login-card {
            background: var(--login-card-bg);
            border-radius: var(--md-sys-shape-corner-extra-large);
            padding: var(--md-sys-spacing-padding-xl);
            box-shadow: var(--md-sys-elevation-level4);
            backdrop-filter: blur(10px);
        }
        
        .login-header {
            text-align: center;
            margin-bottom: var(--md-sys-spacing-margin-xl);
        }
        
        .login-logo {
            font-size: 2rem;
            font-weight: var(--md-sys-typescale-weight-bold);
            color: var(--md-sys-color-primary);
            margin-bottom: var(--md-sys-spacing-margin-md);
        }
        
        .login-title {
            font-size: var(--md-sys-typescale-headline-small-size);
            font-weight: var(--md-sys-typescale-weight-medium);
            color: var(--md-sys-color-on-surface);
            margin-bottom: var(--md-sys-spacing-margin-sm);
        }
        
        .login-subtitle {
            font-size: var(--md-sys-typescale-body-medium-size);
            color: var(--md-sys-color-on-surface-variant);
        }
        
        .form-group {
            margin-bottom: var(--md-sys-spacing-margin-lg);
        }
        
        /* 修复MD3输入框文字重叠问题 */
        .md-input-wrapper {
            margin-bottom: var(--md-sys-spacing-margin-md) !important;
            overflow: hidden; /* 防止内容溢出 */
            position: relative;
        }
        
        /* 填充式输入框样式修复 */
        .md-input-wrapper--filled {
            background-color: var(--md-sys-color-surface-variant);
            border-top-left-radius: var(--md-sys-shape-corner-extra-large);
            border-top-right-radius: var(--md-sys-shape-corner-extra-large);
            min-height: 56px;
        }
        
        .md-input-wrapper--filled .md-input {
            padding: 24px 16px 8px 48px !important; /* 左侧为图标留出足够空间 */
            padding-top: 32px !important; /* 顶部为标签留出空间 */
            box-sizing: border-box;
            width: 100%;
            border: none;
            outline: none;
            background: transparent;
            font-family: inherit;
            font-size: var(--md-sys-typescale-body-large-size);
            color: var(--md-sys-color-on-surface);
            line-height: 1.4;
        }
        
        /* 密码输入框右侧留出空间 */
        .md-input-wrapper--filled .md-input[type="password"] {
            padding-right: 48px !important;
        }
        
        /* 标签样式修复 - 使用组件统一样式 */
        .md-input-wrapper--filled .md-input__label {
            position: absolute;
            top: 24px;
            left: 48px !important;
            right: 16px !important;
            color: var(--md-sys-color-on-surface-variant);
            font-size: var(--md-sys-typescale-body-large-size);
            font-weight: var(--md-sys-typescale-weight-regular);
            pointer-events: none;
            transition: all var(--md-sys-motion-duration-short2) var(--md-sys-motion-easing-standard);
            z-index: 3;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            background: transparent;
            padding: 0;
            margin: 0;
            line-height: 1.2;
            transform-origin: left top;
            box-sizing: border-box;
        }
        
        /* 焦点状态和有值状态的标签 - 完善所有情况 */
        .md-input-wrapper--filled .md-input:focus ~ .md-input__label,
        .md-input-wrapper--filled .md-input:not(:placeholder-shown) ~ .md-input__label,
        .md-input-wrapper--filled[data-has-value="true"] .md-input__label,
        .md-input-wrapper--filled .md-input[value]:not([value=""]) ~ .md-input__label {
            top: 8px;
            font-size: var(--md-sys-typescale-body-small-size);
            color: var(--md-sys-color-primary);
            transform: scale(0.85);
        }
        
        /* 图标样式修复 */
        .md-input__icon {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            color: var(--md-sys-color-on-surface-variant);
            font-size: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 2;
            pointer-events: auto;
            width: 24px;
            height: 24px;
        }
        
        .md-input__icon--leading {
            left: 16px;
        }
        
        .md-input__icon--trailing {
            right: 16px;
            cursor: pointer;
        }
        
        /* 确保容器尺寸 */
        .md-input-wrapper {
            min-height: 56px;
            max-width: 100%;
        }
        

        
        /* 卡片内部滚动保护 */
        .login-card {
            overflow: hidden;
        }
        
        /* 输入框容器的最大宽度限制 */
        .form-group {
            max-width: 100%;
            overflow: hidden;
        }
        
        /* 确保MD3组件正确渲染 */
        .md-input-wrapper {
            max-width: 100%;
            box-sizing: border-box;
        }
        
        .form-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: var(--md-sys-spacing-margin-lg);
            margin-bottom: var(--md-sys-spacing-margin-lg);
        }
        
        .remember-me {
            display: flex;
            align-items: center;
            gap: var(--md-sys-spacing-padding-sm);
        }
        
        .remember-me input {
            width: 18px;
            height: 18px;
        }
        
        .remember-me label {
            font-size: var(--md-sys-typescale-body-small-size);
            color: var(--md-sys-color-on-surface-variant);
        }
        
        .forgot-password {
            font-size: var(--md-sys-typescale-body-small-size);
            color: var(--md-sys-color-primary);
            text-decoration: none;
        }
        
        .forgot-password:hover {
            text-decoration: underline;
        }
        
        .switch-link {
            text-align: center;
            padding-top: var(--md-sys-spacing-padding-lg);
            border-top: 1px solid var(--md-sys-color-outline-variant);
            margin-top: var(--md-sys-spacing-margin-lg);
        }
        
        .switch-link a {
            color: var(--md-sys-color-primary);
            text-decoration: none;
            font-weight: var(--md-sys-typescale-weight-medium);
        }
        
        .switch-link a:hover {
            text-decoration: underline;
        }
        
        /* Demo模式样式 */
        .demo-selector {
            margin-bottom: var(--md-sys-spacing-margin-lg);
        }
        
        .demo-selector select {
            width: 100%;
            padding: var(--md-sys-spacing-padding-md);
            border-radius: var(--md-sys-shape-corner-small);
            border: 1px solid var(--md-sys-color-outline);
            background: var(--md-sys-color-surface-variant);
            color: var(--md-sys-color-on-surface);
        }
        
        /* 响应式设计 */
        @media (max-width: 480px) {
            .login-container {
                padding: 16px;
            }
            
            .login-card {
                padding: var(--md-sys-spacing-padding-lg);
            }
            
            .form-footer {
                flex-direction: column;
                gap: var(--md-sys-spacing-padding-md);
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-card">
            <!-- 头部 -->
            <div class="login-header">
                <div class="login-logo">ZASCA</div>
                <h1 class="login-title">欢迎回来</h1>
                <p class="login-subtitle">登录您的账户以继续</p>
            </div>
            
            <!-- 消息提示 -->
            {% if messages %}
                {% for message in messages %}
                    {% include 'components/alert.html' with type=message.tags|default:"info" dismissible=true content=message %}
                {% endfor %}
            {% endif %}
            
            <!-- 表单 -->
            <form method="post" action="{% url 'accounts:login' %}" class="signin-form" id="loginForm" novalidate>
                {% csrf_token %}
                
                <!-- Demo用户选择器 -->
                {% if is_demo_mode %}
                <div class="demo-selector">
                    <select id="user-selector" class="form-control">
                        <option value="">-- 选择DEMO用户 (留空则手动输入) --</option>
                        <option value="User">User (demo)</option>
                        <option value="Admin">Admin (demo)</option>
                    </select>
                </div>
                {% endif %}
                
                <!-- 用户名输入 -->
                <div class="form-group">
                    {% include 'components/input.html' with type="text" name=form.username.html_name id="username-field" label="用户名" value=form.username.value|default:'' error=form.username.errors|first icon="person" required=true variant="filled" %}
                </div>
                
                <!-- 密码输入 -->
                <div class="form-group">
                    {% include 'components/input.html' with type="password" name=form.password.html_name id="password-field" label="密码" error=form.password.errors|first icon="lock" trailing_icon="visibility" required=true variant="filled" %}
                </div>
                
                <!-- 记住我和忘记密码 -->
                <div class="form-footer">
                    <div class="remember-me">
                        <input type="checkbox" 
                               name="{{ form.remember.html_name }}" 
                               id="remember-checkbox"
                               {% if form.remember.value %}checked{% endif %}>
                        <label for="remember-checkbox">记住我</label>
                    </div>
                    <a href="{% url 'accounts:forgot_password' %}" class="forgot-password">忘记密码？</a>
                </div>
                
                <!-- 登录按钮 -->
                <div class="form-group">
                    {% if CAPTCHA_PROVIDER == 'geetest' %}
                        {% include 'components/button.html' with variant="filled" type="submit" text="登录" classes="w-100" icon="login" attributes='data-geetest-trigger="true"' %}
                    {% elif CAPTCHA_PROVIDER == 'turnstile' %}
                        {% include 'components/button.html' with variant="filled" type="submit" text="登录" classes="w-100" icon="login" %}
                    {% elif CAPTCHA_PROVIDER == 'local' %}
                        {% include 'components/button.html' with variant="filled" type="button" text="登录" classes="w-100" icon="login" id="login-button" %}
                    {% else %}
                        {% include 'components/button.html' with variant="filled" type="submit" text="登录" classes="w-100" icon="login" %}
                    {% endif %}
                </div>
                
                <!-- 隐藏字段 -->
                <input type="hidden" name="lot_number" id="lot_number_login">
                <input type="hidden" name="captcha_output" id="captcha_output_login">
                <input type="hidden" name="pass_token" id="pass_token_login">
                <input type="hidden" name="gen_time" id="gen_time_login">
                <input type="hidden" name="captcha_id" id="captcha_id_login" value="{{ GEETEST_ID }}">
            </form>
            
            <!-- 切换链接 -->
            <div class="switch-link">
                <p>没有账户？ <a href="{% url 'accounts:register' %}">立即注册</a></p>
            </div>
        </div>
    </div>

    <!-- JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="/static/js/base.js"></script>
    <script src="/static/js/accounts.js"></script>
    <script src="/static/js/gt4.js"></script>
    
    <!-- 设置验证码变量 -->
    <script>
        window.CAPTCHA_PROVIDER = '{{ CAPTCHA_PROVIDER|escapejs }}';
        {% if CAPTCHA_PROVIDER == 'geetest' %}
        window.GEETEST_CAPTCHA_ID = '{{ GEETEST_ID|escapejs }}';
        {% elif CAPTCHA_PROVIDER == 'turnstile' %}
        window.TURNSTILE_SITE_KEY = '{{ TURNSTILE_SITE_KEY|escapejs }}';
        {% endif %}
    </script>
    
    <!-- 验证码适配器 -->
    {% if CAPTCHA_PROVIDER == 'geetest' %}
    <script src="/static/js/geetest_v4_adapter.js"></script>
    {% elif CAPTCHA_PROVIDER == 'local' %}
    <script src="/static/js/local_captcha_adapter.js"></script>
    {% elif CAPTCHA_PROVIDER == 'turnstile' %}
    <script src="/static/js/turnstile_adapter.js"></script>
    {% endif %}
    
    <script src="/static/js/email_code.js"></script>

    <script>
        // Demo模式处理
        const isDemoMode = document.documentElement.getAttribute('data-demo-mode') === 'true';
        
        if (isDemoMode) {
            const demoPasswords = {
                'User': 'demo_user_password',
                'Admin': 'demo_admin_password'
            };

            $('#user-selector').on('change', function() {
                const selectedUser = $(this).val();
                if (selectedUser) {
                    $('#username-field').val(selectedUser);
                    $('#password-field').val(demoPasswords[selectedUser] || '');
                } else {
                    $('#username-field').val('');
                    $('#password-field').val('');
                }
            });
        }

        // 密码可见性切换
        document.addEventListener('click', function(e) {
            if (e.target.classList.contains('md-input__icon--trailing')) {
                const input = e.target.closest('.md-input-wrapper').querySelector('.md-input');
                const icon = e.target;
                
                if (input.type === 'password') {
                    input.type = 'text';
                    icon.textContent = 'visibility_off';
                } else {
                    input.type = 'password';
                    icon.textContent = 'visibility';
                }
            }
        });

        // 表单提交处理
        document.getElementById('loginForm')?.addEventListener('submit', function(e) {
            const submitBtn = this.querySelector('[type="submit"]');
            if (submitBtn) {
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<span class="material-icons">hourglass_empty</span> 登录中...';
            }
        });
    </script>
</body>
</html>